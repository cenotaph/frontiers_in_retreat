!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}/
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}/
    %title= content_for?(:title) ? yield(:title) : "frontiers CMS" 
    = stylesheet_link_tag    "admin" 
    = javascript_include_tag "vendor/modernizr" 
    = csrf_meta_tags 
  %body
    .row
      .twelve.columns
        - if flash[:error]
          .error= flash[:error]
        
      
        - if flash[:notice]
          .notice= flash[:notice]
      
        - if flash[:alert]
          .alert= flash[:alert]

    = yield 
    = javascript_include_tag "admin" 